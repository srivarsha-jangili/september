n=2
res="1"
for i in range(n-1):
    i=0
    temp=" "
    while i<len(res):
        count=1
        while i+1<len(res) and res[i+1]==res[i]:
            count+=1
            i+=1
        temp+=str(count)+res[i]
        i+=1
    res=temp
    print(res)